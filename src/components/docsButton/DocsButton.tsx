import AutoStoriesIcon from '@mui/icons-material/AutoStories';
import { IconButton } from '@mui/material';

export function DocsButton() {
  return (
    <IconButton aria-label='query' size='large'>
      <AutoStoriesIcon fontSize='inherit' />
    </IconButton>
  );
}
